# spring configuration
spring:
   application:
      name: ajax-practices-backend

   devtools:
      restart:
         enabled: true		
      livereload:
         enabled: true

# server configuration
server:
   port: 8080
   servlet:
      context-path: /